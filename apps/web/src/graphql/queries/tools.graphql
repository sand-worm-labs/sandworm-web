query GetTools {
  getTools {
    toolId
    categoryId
    name
    description
    tags
    params
  }
}

query RenderToolSource($toolId: String!, $inputs: JSON!) {
  renderToolSource(toolId: $toolId, inputs: $inputs)
}
