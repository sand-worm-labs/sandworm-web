## Database Seeders

We use **TypeORM Extension** to manage database seeders. All seeders are stored in the `src/seeds/` folder.

### Creating a Seeder

Use the CLI command:

```bash
pnpm typeorm-ex seed:create --name src/seeds/SeederName